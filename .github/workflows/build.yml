name: build
on: [push]

jobs:
  test:
    name: Run unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-python@v1
        with:
          python-version: '3.7'
          architecture: 'x64'
      - run: python --version
      - run: pip install pytest
      - run: PYTHONPATH=markdown-helper:$PYTHONPATH pytest